#!/bin/sh

# Copy /tmp/oem folder to /target.
# This folder is used for tuning oem features.

if [ -d /tmp/oem ]; then
  cp -rf /tmp/oem /target/tmp/oem || error "Failed to copy /tmp/oem to /target"
fi